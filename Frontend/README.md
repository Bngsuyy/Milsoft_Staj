# Frontend

React 19 + TypeScript + Vite + PrimeReact ile geliştirilen istemci uygulaması
`TaskManagement.Web/` klasöründedir.

```powershell
cd TaskManagement.Web
npm install
npm run dev
```

Ayrıntılar için `TaskManagement.Web/README.md` ve depo kökündeki `README.md` dosyalarına bakın.
